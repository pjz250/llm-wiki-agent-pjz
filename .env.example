# ============================================================
# LLM Wiki Agent 配置文件
# 复制本文件为 .env，然后修改对应的值即可生效
# cp .env.example .env
# ============================================================

# ---- 模型选择 ----
#
# 格式：提供商/模型名
# litellm 会根据「提供商前缀」自动路由到对应的 API 地址，
# 你不需要手动填写 URL。
#
# 举例：
#   deepseek/deepseek-chat   → 自动路由到 https://api.deepseek.com/v1
#   openai/gpt-4o            → 自动路由到 https://api.openai.com/v1
#   anthropic/claude-sonnet  → 自动路由到 https://api.anthropic.com/v1
#
# 可选模型：
#   DeepSeek:    deepseek/deepseek-chat  或 deepseek/deepseek-reasoner
#   OpenAI:      openai/gpt-4o           或 openai/gpt-4o-mini
#   Anthropic:   anthropic/claude-3-5-sonnet-latest
#   Google:      gemini/gemini-2.0-flash
#
# 如果用第三方中转站或自建网关（兼容 OpenAI 格式）：
#   模型名用 openai/ 前缀，再设置 OPENAI_API_BASE
#   例如：LLM_MODEL=openai/deepseek-chat
#         OPENAI_API_BASE=https://你的中转站地址/v1

# 主力模型（用于收录、查询、lint 等需要强理解力的任务）
LLM_MODEL=deepseek/deepseek-v4-flash

# 快速模型（用于检索、图谱推断等简单任务，可以跟主力设同一个）
LLM_MODEL_FAST=deepseek/deepseek-v4-flash

# ---- API Key ----
# 根据你选择的模型，设置对应的 API Key（不需要的可以注释掉）

# DeepSeek
DEEPSEEK_API_KEY=sk-xxx

# OpenAI / 兼容 OpenAI 格式的第三方服务
# OPENAI_API_KEY=sk-your-openai-key

# Anthropic
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key

# Google Gemini
# GEMINI_API_KEY=your-gemini-key

# ---- 自定义 API 地址（使用第三方中转或自建网关时需要） ----
# 仅当你用 openai/ 前缀 + 非官方地址时才需要设这个
# 官方提供商（deepseek/、openai/、anthropic/、gemini/）不需要
# OPENAI_API_BASE=https://api.deepseek.com/v1
