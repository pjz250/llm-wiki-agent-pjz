查询知识维基并综合给出答案。

用法：/wiki-query $参数

$参数 是要回答的问题，例如 `所有资料的核心主题是什么？`

遵循 CLAUDE.md 中定义的查询工作流：
1. 读取 wiki/index.md 找出最相关的页面
2. 读取这些页面（最多 ~10 个最相关的）
3. 综合出一份完整的 Markdown 答案，使用 [[页面名称]] 维基链接引用
4. 在末尾添加 ## 参考来源 小节，列出参考了哪些页面
5. 询问用户是否要把答案保存到 wiki/syntheses/<slug>.md

如果 wiki 是空的，告诉用户并建议先运行 /wiki-ingest。
